let number = 0;
if (number > 0) {
    console.log(`${number} is a positive number`);

} else if (number == 0) {
    console.log(`${number} is a zero number`);

} else if (number < 0) {
    console.log(`${number} is a negative number`);
} else {
    console.log(`${number} is a other number`);
}